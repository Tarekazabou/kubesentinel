apiVersion: v1
kind: Pod
metadata:
  name: insecure-pod
  namespace: default
  labels:
    app: demo
    environment: test
spec:
  containers:
  - name: nginx
    image: nginx:latest  # VIOLATION: Using 'latest' tag
    ports:
    - containerPort: 80
    - containerPort: 22  # VIOLATION: SSH port exposed
    securityContext:
      privileged: true   # VIOLATION: Privileged container
      runAsNonRoot: false # VIOLATION: Running as root
      readOnlyRootFilesystem: false  # VIOLATION: Writable root filesystem
    # VIOLATION: Missing resource limits
    # VIOLATION: Missing resource requests
